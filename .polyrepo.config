# Polyrepo Workspace Configuration
# Simple format: KEY=VALUE (bash-sourceable)

# Git user config
GIT_USER_EMAIL="naga@workspace.local"
GIT_USER_NAME="Naga"

# GitHub/GitLab base URL and workspace repo
REPO_TYPE="github"  # github, gitlab, bitbucket
BASE_URL="https://github.com/nagabhushanreddy"
WORKSPACE_REPO="workspace"

# Frontend repos (space-separated)
FRONTEND_REPOS="mobile-app"

# Service repos (space-separated)
SERVICE_REPOS="authentication-api authorization-api entity-api reporting-api"

# Infrastructure repos (space-separated)
INFRA_REPOS="infra"
